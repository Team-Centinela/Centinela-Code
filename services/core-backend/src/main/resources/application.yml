spring:
  application:
    name: centinela-core-backend

  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:centinela}
    username: ${DB_USER:postgres}
    password: ${DB_PASSWORD:postgres}
    hikari:
      schema: public

  flyway:
    enabled: true
    schemas: cases,alerts,reporting,auth,rules_config,outbox
    create-schemas: true
    locations: classpath:db/migration
    # Disabled so Flyway runs V1..V3 from version 0 against a fresh database
    # (init.sql pre-creates the schemas, which `true` would interpret as
    # "non-empty, baseline instead"). Tracked by #176.
    baseline-on-migrate: false

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        default_schema: public

  cloud:
    stream:
      bindings:
        transactionsRaw-in-0:
          destination: transactions-raw
          group: core-backend
        fraud-evaluation-out-0:
          destination: fraud-evaluation
    azure:
      servicebus:
        connection-string: ${AZURE_SERVICEBUS_CONNECTION_STRING:}
        processor:
          auto-startup: false

server:
  port: 8080

outbox:
  publisher:
    interval-ms: 1000
    batch-size: 100
    max-attempts: 10
    stale-threshold-minutes: 5
    drain-timeout-ms: 10000

logging:
  level:
    com.centinela: DEBUG
