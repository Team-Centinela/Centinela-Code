# Local tooling — never commit
.vscode/
.obsidian/

# Obsidian plugin mirror folders — DO NOT commit
# These duplicate GitHub Issues and Pull Requests. Source of truth is GitHub.
GitHub/
GitHub-PR/
"GitHub Pull Requests/"

# Local secrets / scratch
*.env
*.local

# Local context compression snapshots created by the AI agent workflow
# (see docs/AGENTS.md §"Context compression ritual"). They never leave the
# developer's machine.
.context-snapshots/
context-snapshot-*.md

# Targeted build / dependency outputs (per service language)

# Java / Maven / Gradle
target/
build/
*.class
*.jar
.gradle/
out/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.pytest_cache/
.ruff_cache/

# Node — Frontend
node_modules/
dist/
.parcel-cache/

# Terraform
.terraform/
*.tfplan
*.tfstate
*.tfstate.backup
.terraform.lock.hcl.bak
